info:
  name: LOGIN TEST
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/auth/login
  body:
    type: json
    data: |
      {
        "email": "mentor@insight.com",
        "id": 777
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
