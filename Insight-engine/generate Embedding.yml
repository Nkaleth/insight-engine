info:
  name: generate Embedding
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/ai/embed
  body:
    type: json
    data: |
      {
        "text": "Odio profundamente cuando la interfaz de usuario no es intuitiva"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
